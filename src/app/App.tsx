import AppGrid from '../AppGrid/AppGrid';
import { AppRoot } from '../appRoot/AppRoot';
import './styles.module.css';

export const App = () => {
  return (
    <AppRoot>
      <AppGrid />
    </AppRoot>
  );
};
